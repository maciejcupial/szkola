# Task 1 of three: decide whether a number is even or odd.
# The student replaces the TU ZMIEŃ line with an if/else block.
# main.py in the folder above shows a working if/else to copy the shape from.

# input() returns text, int() turns that text into a whole number.
number = int(input("Podaj liczbę: "))

# The % operator gives the remainder of a division: 10 % 2 is 0, 7 % 2 is 1.
# A number is even when the remainder of dividing it by 2 equals 0.
# Use == to compare two values (a single = would assign instead of compare).

# TU ZMIEŃ: warunek if / else, który wypisze „Liczba parzysta” albo „Liczba nieparzysta”
print("Uzupełnij warunek w pliku 01_condition.py")
