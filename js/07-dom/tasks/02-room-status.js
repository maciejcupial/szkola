// Task 2 of 5: setBusy() and setFree() change text and colour and count changes.
// Expected after „Zajęta”, „Wolna”, „Zajęta”:
//   status:   Sala zajęta   (red)
//   changes:  Zmian statusu: 3

// Outside the functions, so it keeps its value between clicks.
let changes = 0;

function setBusy() {
  // TU ZMIEŃ: znajdź akapit "status", wpisz textContent "Sala zajęta" i ustaw style.color "red"
  // TU ZMIEŃ: zwiększ changes o 1 i wpisz do akapitu "changes" tekst "Zmian statusu: " + changes
}

function setFree() {
  // TU ZMIEŃ: to samo z tekstem "Sala wolna" i kolorem "green"
}
