// Task 5 of 6: add up even numbers with continue, stop with break when sum > 100.
// Expected: "Dodaję 2, suma 2" ... "Dodaję 20, suma 110", then: Stop na liczbie 20, suma 110

let sum = 0;
let lastNumber = 0;

for (let i = 1; i <= 40; i++) {
  // TU ZMIEŃ: continue dla nieparzystych, dodaj do sum, zapisz w lastNumber, wypisz, break po 100
}

console.log(`Stop na liczbie ${lastNumber}, suma ${sum}`);
